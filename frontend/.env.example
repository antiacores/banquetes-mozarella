# Copia este archivo como ".env" y ajusta según tu entorno.

# URL del backend. En desarrollo local, deja la de localhost.
# Cuando subas el backend a Render, cámbiala por esa URL pública.
VITE_API_URL=http://localhost:8000
